# API Configuration
VITE_API_URL=http://localhost:8000

# Authentication
# Format: "username1:password1:role,username2:password2:role"
# Role values: admin | read_only
# Backward compatible with old "username:password" (defaults to read_only)
VITE_DASH_USERS=admin:admin:admin,viewer:viewer:read_only
